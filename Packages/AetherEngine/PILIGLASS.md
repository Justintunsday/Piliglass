# AetherEngine vendored source

Pinned from upstream https://github.com/superuser404notfound/AetherEngine
tag `6.39.0` (commit f70d4aee595f0947ef2354fbae01c76b41a7857b), which is the
exact version the iOS app locked via
`ios/Runner.xcodeproj/project.pbxproj` (`XCLocalSwiftPackageReference`).

Local changes made for PiliGlass playback fixes live in this copy so they can
be reviewed and built by CI without upstream drift.
